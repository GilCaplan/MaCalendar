"""Concrete training programs — session content, with preferred dates only.

Every date in here is re-placed by `workout_plan.schedule()` against the
observance rules before it reaches the calendar.
"""
