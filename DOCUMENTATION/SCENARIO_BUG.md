# Scenario Bug Log

Auto-generated by the LLM-as-judge background verifier.
Each entry represents a case where the rule-based fast-path was wrong.
Review these to add regression tests or improve rules.
When fixing bugs, append as a test case to the test file to verify that you fixed, can skip the transcription part and test the scenario deeply, this files gets updated automatically when bugs are found. See if it makes sense and possible to fix the system/algorithm given these bugs/scenarios. We also don't want to completely overfit to these bugs, try think about the general case they represent so we can prevent other bugs moving forward.
P.S can remove a bug once we fixed from this md file.


